name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  distribution:
    permissions:
      contents: write
    uses: brainage04/FabricModdingConventions/.github/workflows/reusable-multiloader-release.yml@v2.4.0
    with:
      additional_artifact_pattern: vein_miner-neoforge-*.jar
      release_name: ${{ format('Vein Miner {0}', github.ref_name) }}
      modrinth_project_id: ${{ vars.MODRINTH_PROJECT_ID }}
      curseforge_project_id: ${{ vars.CURSEFORGE_PROJECT_ID }}
      curseforge_project_slug: vein-miner
    secrets:
      release_token: ${{ secrets.GITHUB_TOKEN }}
      modrinth_token: ${{ secrets.MODRINTH_TOKEN }}
      curseforge_token: ${{ secrets.CURSEFORGE_TOKEN }}
